/*
main() {
  testWidgets('BenefitPage has title', (WidgetTester tester) async {
    await tester.pumpWidget(buildTestableWidget(BenefitPage(title: 'Benefit')));
    final titleFinder = find.text('Benefit');
    expect(titleFinder, findsOneWidget);
  });
}
*/
