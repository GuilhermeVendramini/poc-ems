/*void main() {
  initModule(BenefitModule());
  BenefitController benefit;

  setUp(() {
    benefit = BenefitModule.to.get<BenefitController>();
  });

  group('BenefitController Test', () {
    test("First Test", () {
      expect(benefit, isInstanceOf<BenefitController>());
    });

    test("Set Value", () {
      expect(benefit.value, equals(0));
      benefit.increment();
      expect(benefit.value, equals(1));
    });
  });
}*/
