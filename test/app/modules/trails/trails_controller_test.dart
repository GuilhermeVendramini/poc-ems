/*
import 'package:ems/app/modules/trails/trails_controller.dart';
import 'package:ems/app/modules/trails/trails_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(TrailsModule());
  TrailsController trails;

  setUp(() {
    trails = TrailsModule.to.get<TrailsController>();
  });

  group('TrailsController Test', () {
    test("First Test", () {
      expect(trails, isInstanceOf<TrailsController>());
    });

    test("Set Value", () {
      expect(trails.value, equals(0));
      trails.increment();
      expect(trails.value, equals(1));
    });
  });
}
*/
