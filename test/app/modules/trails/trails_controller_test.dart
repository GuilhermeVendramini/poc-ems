/*
import 'package:ems/app/modules/app.services.trails/trails_controller.dart';
import 'package:ems/app/modules/app.services.trails/trails_module.dart';
import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  initModule(TrailsModule());
  TrailsController app.services.trails;

  setUp(() {
    app.services.trails = TrailsModule.to.get<TrailsController>();
  });

  group('TrailsController Test', () {
    test("First Test", () {
      expect(app.services.trails, isInstanceOf<TrailsController>());
    });

    test("Set Value", () {
      expect(app.services.trails.value, equals(0));
      app.services.trails.increment();
      expect(app.services.trails.value, equals(1));
    });
  });
}
*/
