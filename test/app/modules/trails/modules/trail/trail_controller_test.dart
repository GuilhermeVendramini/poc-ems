/*void main() {
  initModule(TrailModule());
  TrailController trail;

  setUp(() {
    trail = TrailModule.to.get<TrailController>();
  });

  group('TrailController Test', () {
    test("First Test", () {
      expect(trail, isInstanceOf<TrailController>());
    });

    test("Set Value", () {
      expect(trail.value, equals(0));
      trail.increment();
      expect(trail.value, equals(1));
    });
  });
}*/
