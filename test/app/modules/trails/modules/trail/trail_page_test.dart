/*
main() {
  testWidgets('TrailPage has title', (WidgetTester tester) async {
    await tester.pumpWidget(buildTestableWidget(TrailPage(title: 'Trail')));
    final titleFinder = find.text('Trail');
    expect(titleFinder, findsOneWidget);
  });
}
*/
